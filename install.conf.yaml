- defaults:
    link:
      relink: true

- link:
    ~/.zshrc: zshrc
    ~/.zshrc_user: zshrc_user
    ~/.tmux.conf: tmux.conf
    ~/.config/nvim: nvim
    ~/.aliases: aliases
    ~/startup.m: startup.m
